name: correspondence_soco
random_seed: 8
data_root: ${oc.env:SOCO_ROOT,data/SOCOv1}
pair_subdir: PairAnnotations/intra
image_size: 800
num_patches: 60
fixed_patched_size: false
use_bbox: false
classes: all
max_pairs: null
thresh: 0.10
mask_feats: false
multilayer: false
num_visualize: 0
soft_eval: false
soft_eval_beta: 0.02
soft_eval_window: 7
reduction: pair
runner:
  module: omniprobe.scripts.eval_correspondence_soco
  required_output: dense
