random_seed: 8
scale_factor: 0.25
num_corr: 1000
multilayer: False
defaults:
  - backbone: dinov2_b14
  - dataset: navi

task_name: correspondence_navi

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}_${task_name}_${hydra:runtime.choices.backbone}
