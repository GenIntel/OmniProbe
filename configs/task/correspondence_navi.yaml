defaults:
  - /dataset: navi
  - _self_

name: correspondence_navi
random_seed: 8
scale_factor: 0.25
num_corr: 1000
multilayer: false
runner:
  module: omniprobe.scripts.eval_correspondence_navi
  required_output: dense
